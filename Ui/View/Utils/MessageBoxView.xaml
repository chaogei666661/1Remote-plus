<styles:WindowChromeBase x:Class="_1RM.View.Utils.MessageBoxView"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                  xmlns:utils="clr-namespace:_1RM.View.Utils"
                  xmlns:styles="clr-namespace:Shawn.Utils.WpfResources.Theme.Styles;assembly=Shawn.Utils.WpfResources"
                  xmlns:s="https://github.com/canton7/Stylet"
                  mc:Ignorable="d"
                  d:DataContext="{d:DesignInstance utils:MessageBoxViewModel}"
                  ResizeMode="NoResize"
                  SizeToContent="WidthAndHeight"
                  WindowStyle="None"
                  Background="Transparent"
                  Topmost="True"
                  AllowsTransparency="True"
                  FlowDirection="{Binding PageViewModel.FlowDirection}"
                  ShowInTaskbar="False"
                  MinWidth="160" MaxWidth="485">
    <!-- The card has no window chrome and its buttons are not focusable, so Escape has to be caught on the
         window itself. It routes to the cancel button, never to the affirmative one. -->
    <styles:WindowChromeBase.InputBindings>
        <KeyBinding Key="Escape" Command="{Binding CmdCancel}"></KeyBinding>
    </styles:WindowChromeBase.InputBindings>
    <ContentControl s:View.Model="{Binding PageViewModel}"></ContentControl>
</styles:WindowChromeBase>