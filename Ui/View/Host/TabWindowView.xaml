<styles:WindowChromeBase x:Class="_1RM.View.Host.TabWindowView"
                       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                       xmlns:dragablz="http://dragablz.net/winfx/xaml/dragablz"
                       xmlns:attachProperty="clr-namespace:Shawn.Utils.WpfResources.Theme.AttachProperty;assembly=Shawn.Utils.WpfResources"
                       xmlns:controls="clr-namespace:Shawn.Utils.Wpf.Controls;assembly=Shawn.Utils.Wpf"
                       xmlns:host="clr-namespace:_1RM.View.Host"
                       xmlns:s="https://github.com/canton7/Stylet"
                       xmlns:styles="clr-namespace:Shawn.Utils.WpfResources.Theme.Styles;assembly=Shawn.Utils.WpfResources"
                       mc:Ignorable="d"
                       d:DataContext="{d:DesignInstance host:TabWindowViewModel}"
                       ResizeMode="{Binding WindowResizeMode}"
                       BorderThickness="0"
                       BorderBrush="Transparent"
                       Background="{DynamicResource WindowBackdropBrush}"
                       Title="{Binding Title}" Height="600" Width="800">
    <!-- AcrylicBehavior is intentionally omitted, and this type is on its denylist as well. Remote content
         is a WindowsFormsHost child HWND that paints with GDI, which leaves the alpha byte at zero; the
         accent policy honours per-pixel alpha across the whole window, so frosting the chrome also blurs
         the desktop through the session as a white fog. The title strip fakes the material instead — see
         the Border at the top of the root Grid. -->
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" GlassFrameThickness="1" ResizeBorderThickness="5" UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>
    <Window.InputBindings>
        <KeyBinding Gesture="Ctrl+1" Command="{Binding CmdShowTabByIndex}" CommandParameter="1"></KeyBinding>
        <KeyBinding Gesture="Ctrl+2" Command="{Binding CmdShowTabByIndex}" CommandParameter="2"></KeyBinding>
        <KeyBinding Gesture="Ctrl+3" Command="{Binding CmdShowTabByIndex}" CommandParameter="3"></KeyBinding>
        <KeyBinding Gesture="Ctrl+4" Command="{Binding CmdShowTabByIndex}" CommandParameter="4"></KeyBinding>
        <KeyBinding Gesture="Ctrl+5" Command="{Binding CmdShowTabByIndex}" CommandParameter="5"></KeyBinding>
        <KeyBinding Gesture="Ctrl+6" Command="{Binding CmdShowTabByIndex}" CommandParameter="6"></KeyBinding>
        <KeyBinding Gesture="Ctrl+7" Command="{Binding CmdShowTabByIndex}" CommandParameter="7"></KeyBinding>
        <KeyBinding Gesture="Ctrl+8" Command="{Binding CmdShowTabByIndex}" CommandParameter="8"></KeyBinding>
        <KeyBinding Gesture="Ctrl+9" Command="{Binding CmdShowTabByIndex}" CommandParameter="9"></KeyBinding>
        <KeyBinding Gesture="F11" Command="{Binding CmdGoMaximizeF11}"></KeyBinding>
    </Window.InputBindings>
    <Window.Template>
        <ControlTemplate TargetType="host:TabWindowView">
            <Border x:Name="WindowBorder" Padding="0" Background="{DynamicResource WindowBackdropBrush}">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="BorderBrush" Value="{DynamicResource CardStrokeBrush}"></Setter>
                        <Setter Property="BorderThickness" Value="1"></Setter>
                    </Style>
                </Border.Style>
                <Grid x:Name="WindowContentGrid" Background="{TemplateBinding Background}">
                    <ContentPresenter />
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="WindowState" Value="Maximized">
                    <!--注意修改这里后，需要修改会影响RDP模块对应的缩放分辨率计算函数的正确性，导致部分用户全屏后RDP显示出滚动条-->
                    <Setter TargetName="WindowContentGrid" Property="Margin" Value="8 8 8 8" />
                    <!--<Setter TargetName="WindowContentGrid" Property="Margin" Value="8 6 6 8" />-->
                    <Setter TargetName="WindowBorder" Property="BorderThickness" Value="0"></Setter>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
    </Window.Template>
    <Window.Resources>
        <ResourceDictionary>
            <Thickness x:Key="TabContentBorderWithColor">3,0,3,3</Thickness>
            <Thickness x:Key="TabContentBorderWithOutColor">0,0,0,0</Thickness>
            <!--Tab header content DataTemplate-->
            <DataTemplate DataType="{x:Type host:TabItemViewModel}">
                <!--MarkColor border l r b -->
                <Border BorderBrush="{Binding ColorHex, Converter={StaticResource ConverterColorHexString2Brush}, Mode=OneWay}">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="BorderThickness" Value="{StaticResource TabContentBorderWithColor}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ColorHex, Converter={StaticResource ConverterColorIsTransparent}}" Value="True">
                                    <Setter Property="BorderThickness" Value="{StaticResource TabContentBorderWithOutColor}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <ContentControl Content="{Binding Content}" Background="Transparent" />
                </Border>
            </DataTemplate>

            <!--Tab header item Template-->
            <Style x:Key="DragableTabHeaderItemStyle" TargetType="{x:Type dragablz:DragablzItem}">
                <Setter Property="FocusVisualStyle" Value="{x:Null}"></Setter>
                <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="Margin" Value="0 0 2 0" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="VerticalContentAlignment" Value="Stretch" />
                <Setter Property="Height" Value="{x:Static host:TabWindowView.TITLE_BAR_HEIGHT}" />
                <Setter Property="VerticalAlignment" Value="Bottom" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type dragablz:DragablzItem}">
                            <Border x:Name="DragablzHeaderItemBorder"
                                    Background="Transparent"
                                    BorderThickness="0 0 0 2"
                                    BorderBrush="Transparent"
                                    CornerRadius="6 6 0 0"
                                    Height="{x:Static host:TabWindowView.TITLE_BAR_HEIGHT}"
                                    VerticalAlignment="Bottom">
                                <Grid x:Name="DragablzHeaderItemContent" FocusManager.IsFocusScope="True" 
                                      Background="Transparent"
                                      SnapsToDevicePixels="true"
                                      Height="{x:Static host:TabWindowView.TITLE_BAR_HEIGHT}"
                                      controls:MouseMiddleClick.MouseMiddleDown="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=host:TabWindowView}, Path=DataContext.CmdClose}"
                                      controls:MouseMiddleClick.MouseMiddleDownParameter="{Binding Content.ConnectionId}"
                                      VerticalAlignment="Bottom">
                                    <Grid.ContextMenu>
                                        <ContextMenu ItemsSource="{Binding Content.MenuItems}">
                                            <ContextMenu.Style>
                                                <Style TargetType="{x:Type ContextMenu}">
                                                    <Setter Property="OverridesDefaultStyle" Value="True" />
                                                    <Setter Property="SnapsToDevicePixels" Value="True" />
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="{x:Type ContextMenu}">
                                                                <Border BorderThickness="1" BorderBrush="{DynamicResource CardStrokeBrush}" CornerRadius="{DynamicResource DefaultCornerRadius}" x:Name="Border" Background="{DynamicResource SolidSurfaceBrush}" Padding="4">
                                                                    <StackPanel ClipToBounds="True" Orientation="Vertical" IsItemsHost="True" />
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </ContextMenu.Style>
                                        </ContextMenu>
                                    </Grid.ContextMenu>
                                    <Grid Name="Trapezoid" Height="{x:Static host:TabWindowView.TITLE_BAR_HEIGHT}" 
                                          ToolTip="{Binding DisplayName, Mode=OneWay}"
                                          VerticalAlignment="Center">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <!--header item icon-->
                                        <Border Grid.Column="1" Padding="6 0 0 0"
                                                Visibility="{Binding Content.SettingsPage.TabHeaderShowIconButton, Converter={StaticResource ConverterBool2Visible}}">
                                            <Grid Width="16" Height="16" VerticalAlignment="Center" HorizontalAlignment="Center">
                                                <Image Source="{Binding IconImg}" Stretch="Uniform"></Image>
                                            </Grid>
                                        </Border>

                                        <!--header item content-->
                                        <Border Grid.Column="2" Padding="0" Margin="6 0 6 0">
                                            <TextBlock HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"
                                                       TextTrimming="CharacterEllipsis"
                                                       MaxWidth="250"
                                                       MinWidth="40"
                                                       FontSize="{DynamicResource GlobalFontSizeBody}"
                                                       Text="{Binding DisplayName, Mode=OneWay}"></TextBlock>
                                        </Border>

                                        <!--header item placeholder(do not move or remove this)-->
                                        <Thumb Grid.Column="0" Grid.ColumnSpan="4" HorizontalAlignment="Stretch" VerticalContentAlignment="Stretch" x:Name="PART_Thumb">
                                            <Thumb.Style>
                                                <Style TargetType="{x:Type Thumb}">
                                                    <Setter Property="Background" Value="Transparent" />
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate>
                                                                <Grid Background="{TemplateBinding Background}" />
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </Thumb.Style>
                                        </Thumb>

                                        <Button Grid.Column="4" Width="18" Height="18" 
                                                Visibility="{Binding Content.SettingsPage.TabHeaderShowReConnectButton, Converter={StaticResource ConverterBool2Visible}}"
                                                Style="{StaticResource ButtonBarPrimaryStyle}"
                                                Background="Transparent"
                                                attachProperty:VisionElement.Opacity="0.6"
                                                attachProperty:VisionElement.OpacityAccent="1"
                                                attachProperty:VisionElement.BackgroundAccent="Transparent"
                                                ToolTip="{DynamicResource Reconnect}"
                                                Margin="0 0 4 0"
                                                Command="{Binding CmdReconnect}">
                                            <Path Data="{StaticResource GeometryRefresh}" Width="10" Height="10"
                                                  Fill="{Binding Foreground, RelativeSource={RelativeSource FindAncestor, AncestorType=Button}}"
                                                  Stretch="Uniform" />
                                        </Button>

                                        <Button Grid.Column="5" Width="18" Height="18" 
                                                Visibility="{Binding Content.SettingsPage.TabHeaderShowCloseButton, Converter={StaticResource ConverterBool2Visible}}"
                                                Style="{StaticResource ButtonBarPrimaryStyle}"
                                                Background="Transparent"
                                                attachProperty:VisionElement.Opacity="0.6"
                                                attachProperty:VisionElement.OpacityAccent="1"
                                                attachProperty:VisionElement.BackgroundAccent="{DynamicResource LayerHoverBrush}"
                                                ToolTip="{DynamicResource Close}"
                                                Margin="0 0 6 0"
                                                Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=host:TabWindowView}, Path=DataContext.CmdClose}"
                                                CommandParameter="{Binding Content.ConnectionId}">
                                            <Path Data="{StaticResource GeometryCloseCross}" Width="8" Height="8"
                                                  Fill="{Binding Foreground, RelativeSource={RelativeSource FindAncestor, AncestorType=Button}}"
                                                  Stretch="Uniform" />
                                        </Button>
                                    </Grid>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <DataTrigger Binding="{Binding ColorHex, Converter={StaticResource ConverterColorIsTransparent}}" Value="False">
                                    <Setter TargetName="Trapezoid" Property="Margin" Value="2 0" />
                                </DataTrigger>
                                <Trigger Property="IsSelected" Value="False">
                                    <Setter Property="Background" Value="Transparent" TargetName="DragablzHeaderItemBorder" />
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource LayerHoverBrush}" TargetName="DragablzHeaderItemBorder" />
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource LayerSelectedBrush}" TargetName="DragablzHeaderItemBorder" />
                                    <!-- A tab with no colour of its own used to underline itself in the
                                         transparent brush the converter returns, so the only thing marking
                                         the selected tab was a faint fill. -->
                                    <Setter Property="BorderBrush" TargetName="DragablzHeaderItemBorder" Value="{DynamicResource AccentMidBrush}"/>
                                </Trigger>
                                <!-- Declared after the selection trigger so a tab that does carry a colour
                                     keeps showing it rather than the accent. -->
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding IsSelected, RelativeSource={RelativeSource TemplatedParent}}" Value="True" />
                                        <Condition Binding="{Binding ColorHex, Converter={StaticResource ConverterColorIsTransparent}}" Value="False" />
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="BorderBrush" TargetName="DragablzHeaderItemBorder" Value="{Binding ColorHex, Converter={StaticResource ConverterColorHexString2Brush}, Mode=OneWay}"/>
                                </MultiDataTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!--tab header container-->
            <Style x:Key="DragablzTabHeaderItemHolderStryle" TargetType="{x:Type dragablz:DragablzItemsControl}">
                <Setter Property="HorizontalAlignment" Value="Left" />
                <Setter Property="VerticalAlignment" Value="Bottom" />
                <Setter Property="Height" Value="{x:Static host:TabWindowView.TITLE_BAR_HEIGHT}" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="ItemsPanel">
                    <Setter.Value>
                        <ItemsPanelTemplate>
                            <Canvas IsItemsHost="True" />
                        </ItemsPanelTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type dragablz:DragablzItemsControl}">
                            <Grid Background="Transparent"
                                  Height="{x:Static host:TabWindowView.TITLE_BAR_HEIGHT}">
                                <Grid Height="{x:Static host:TabWindowView.TITLE_BAR_HEIGHT}" VerticalAlignment="Bottom">
                                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled"
                                                  Width="{TemplateBinding ActualWidth}"
                                                  Height="{x:Static host:TabWindowView.TITLE_BAR_HEIGHT}"
                                                  VerticalAlignment="Bottom"
                                                  VerticalContentAlignment="Bottom"
                                                  controls:ScrollViewerExtensions.IsHorizontalScrollOnWheelEnabled="true"
                                                  attachProperty:VisionElement.ForegroundAccent="{DynamicResource PrimaryTextBrush}"
                                                  attachProperty:VisionElement.BackgroundAccent="{DynamicResource PrimaryTextBrush}"
                                                  Style="{StaticResource ScrollViewMiniStyle}"
                                                  Background="{x:Null}">
                                        <ItemsPresenter SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}"
                                                        HorizontalAlignment="Left"
                                                        VerticalAlignment="Bottom"
                                                        Width="{TemplateBinding ItemsPresenterWidth}"
                                                        Height="{x:Static host:TabWindowView.TITLE_BAR_HEIGHT}" />
                                    </ScrollViewer>
                                </Grid>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
            <Style TargetType="{x:Type dragablz:TabablzControl}">
                <!--Add button to left of header items-->
                <Setter Property="HeaderPrefixContent">
                    <Setter.Value>
                        <StackPanel Margin="6 0" Height="{x:Static host:TabWindowView.TITLE_BAR_HEIGHT}" 
                                    Orientation="Horizontal"
                                    VerticalAlignment="Center">
                            <StackPanel Visibility="{Binding SettingsPage.GeneralSettingViewModel.TabWindowCloseButtonOnLeft, Converter={StaticResource ConverterBool2Visible}}"
                                        Orientation="Horizontal"
                                        Background="Transparent">
                                <!--IT ONLY A PLACEHOLDER FOR 6 BUTTONS, 4 * 45 + 5 + 45 * 2(depends on this.SelectedItem.Content?.CanFullScreen & BtnCloseAllVisibility)-->
                                <Grid Width="185"></Grid>
                                <Grid Width="45" Visibility="{Binding SelectedItem.Content.CanFullScreen, Converter={StaticResource BooleanToVisibilityConverter}}"></Grid>
                                <Grid Width="45" Visibility="{Binding BtnCloseAllVisibility}"></Grid>
                                <Grid Width="42" Visibility="{Binding SendCommandVisibility}"></Grid>
                            </StackPanel>

                            <Grid Width="18" Height="18" VerticalAlignment="Center">
                                <Image Source="/Resources/Image/Logo/logo32.png"></Image>
                            </Grid>
                        </StackPanel>
                    </Setter.Value>
                </Setter>

                <!--Add button to right of header items-->
                <Setter Property="HeaderSuffixContent">
                    <Setter.Value>
                        <Grid Background="Transparent">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"></ColumnDefinition>
                                <ColumnDefinition Width="Auto"></ColumnDefinition>
                            </Grid.ColumnDefinitions>

                            <Grid Grid.Column="0" Background="Transparent">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                                    <ColumnDefinition Width="*"></ColumnDefinition>
                                </Grid.ColumnDefinitions>

                                <Grid Grid.Column="1" 
                                      MouseDown="WinTitleBar_OnPreviewMouseDown" 
                                      PreviewMouseMove="WinTitleBar_OnPreviewMouseMove" 
                                      Background="Transparent"></Grid>

                                <Button Grid.Column="0" Width="26" Height="22" VerticalAlignment="Center"
                                        Visibility="{Binding SettingsPage.LauncherSettingViewModel.LauncherEnabled, Converter={StaticResource ConverterBool2Visible}}"
                                        Style="{StaticResource WindowCaptionButtonStyle}"
                                        attachProperty:VisionElement.Opacity="0.6"
                                        attachProperty:VisionElement.OpacityAccent="1"
                                        Margin="4 0"
                                        HorizontalAlignment="Left" Command="{Binding CmdInvokeLauncher}">
                                    <Path Data="{StaticResource GeometryCross}"
                                          Width="10" Height="10"
                                          Fill="{Binding Foreground, RelativeSource={RelativeSource FindAncestor, AncestorType=Button}}"
                                          Stretch="Uniform" />
                                </Button>
                            </Grid>

                            <StackPanel Grid.Column="1" 
                                        Visibility="{Binding SettingsPage.GeneralSettingViewModel.TabWindowCloseButtonOnLeft, Converter={StaticResource ConverterBool2VisibleInv}}"
                                        Orientation="Horizontal" HorizontalAlignment="Right"
                                        Background="Transparent">
                                <!--IT ONLY A PLACEHOLDER FOR 5 BUTTONS, 4 * 45 + 5 + 45 * 2(depends on this.SelectedItem.Content?.CanFullScreen & BtnCloseAllVisibility)-->
                                <Grid Width="185"></Grid>
                                <Grid Width="45" Visibility="{Binding SelectedItem.Content.CanFullScreen, Converter={StaticResource BooleanToVisibilityConverter}}"></Grid>
                                <Grid Width="45" Visibility="{Binding BtnCloseAllVisibility}"></Grid>
                                <Grid Width="42" Visibility="{Binding SendCommandVisibility}"></Grid>
                            </StackPanel>

                        </Grid>
                    </Setter.Value>
                </Setter>

                <Setter Property="Background" Value="{DynamicResource SolidSurfaceBrush}"></Setter>
                <Setter Property="ItemContainerStyle" Value="{StaticResource DragableTabHeaderItemStyle}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type dragablz:TabablzControl}">
                            <Grid ClipToBounds="true" SnapsToDevicePixels="true" KeyboardNavigation.TabNavigation="Local">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition x:Name="ColumnDefinition0" />
                                    <ColumnDefinition x:Name="ColumnDefinition1" Width="0" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition x:Name="RowDefinition0" Height="Auto" />
                                    <RowDefinition x:Name="RowDefinition1" Height="*" />
                                </Grid.RowDefinitions>
                                <Border x:Name="contentPanel" 
                                        BorderThickness="0" 
                                        Background="{TemplateBinding Background}"
                                        Grid.Column="0" 
                                        Grid.Row="1"
                                        KeyboardNavigation.DirectionalNavigation="Contained"
                                        KeyboardNavigation.TabIndex="2" 
                                        KeyboardNavigation.TabNavigation="Local">
                                    <Grid>
                                        <Grid x:Name="PART_ItemsHolder" Margin="0" />
                                    </Grid>
                                </Border>
                                <Grid Grid.Column="0" Grid.Row="0" Margin="0" x:Name="HeaderContainerGrid" Visibility="{TemplateBinding IsHeaderPanelVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <ContentControl Grid.Column="0" x:Name="PrefixContentControl"
                                                    Content="{TemplateBinding HeaderPrefixContent}"
                                                    ContentStringFormat="{TemplateBinding HeaderPrefixContentStringFormat}"
                                                    ContentTemplate="{TemplateBinding HeaderPrefixContentStringFormat}"
                                                    ContentTemplateSelector="{TemplateBinding HeaderPrefixContentTemplateSelector}" />
                                    <dragablz:DragablzItemsControl x:Name="PART_HeaderItemsControl"
                                                                   Style="{StaticResource DragablzTabHeaderItemHolderStryle}"
                                                                   Grid.Column="1"
                                                                   ItemsSource="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Items}"
                                                                   ItemContainerStyle="{TemplateBinding ItemContainerStyle}"
                                                                   ItemsOrganiser="{TemplateBinding HeaderItemsOrganiser}"
                                                                   KeyboardNavigation.TabIndex="1" Panel.ZIndex="1"
                                                                   ItemTemplate="{TemplateBinding HeaderItemTemplate}"
                                                                   FixedItemCount="{TemplateBinding FixedHeaderCount}">
                                        <dragablz:DragablzItemsControl.MaxWidth>
                                            <MultiBinding>
                                                <MultiBinding.Converter>
                                                    <dragablz:TabablzHeaderSizeConverter Orientation="Horizontal" />
                                                </MultiBinding.Converter>
                                                <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="ActualWidth" />
                                                <Binding RelativeSource="{RelativeSource Self}" Path="ItemsPresenterWidth" />
                                                <Binding ElementName="PrefixContentControl" Path="ActualWidth" />
                                                <Binding ElementName="DefaultAddButton" Path="DesiredSize.Width" />
                                                <Binding ElementName="SuffixContentControl" Path="DesiredSize.Width" />
                                            </MultiBinding>
                                        </dragablz:DragablzItemsControl.MaxWidth>
                                    </dragablz:DragablzItemsControl>
                                    <Button Content="Add"
                                            x:Name="DefaultAddButton"
                                            Grid.Column="2"
                                            Command="{x:Static dragablz:TabablzControl.AddItemCommand}"
                                            Visibility="{TemplateBinding ShowDefaultAddButton, Converter={StaticResource BooleanToVisibilityConverter}}" />

                                    <ContentControl Grid.Column="3" x:Name="SuffixContentControl"
                                                    Content="{TemplateBinding HeaderSuffixContent}"
                                                    ContentStringFormat="{TemplateBinding HeaderSuffixContentStringFormat}"
                                                    ContentTemplate="{TemplateBinding HeaderSuffixContentStringFormat}"
                                                    ContentTemplateSelector="{TemplateBinding HeaderSuffixContentTemplateSelector}"
                                                    HorizontalAlignment="Stretch" />

                                    <!-- Subtle line separator under the tab header -->
                                    <Border BorderThickness="0 0 0 1"
                                            Grid.Column="0" Grid.ColumnSpan="4"
                                            BorderBrush="{DynamicResource CardStrokeBrush}"
                                            SnapsToDevicePixels="True">
                                    </Border>
                                </Grid>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Background="Transparent">
        <!--
            Simulated glass, not DWM frost, and not GlassPanelBrush either.

            This window stays on AcrylicBehavior's denylist: the session below is a WindowsFormsHost child
            HWND that paints with GDI, GDI leaves the alpha byte at zero, and the accent policy honours
            per-pixel alpha across the whole window — so the one call that would frost this strip also
            turns every session pixel into blurred desktop. That is the white fog, and it is a property of
            the HWND, not of this Border, so no fill here can bring it back.

            With nothing composited behind the window, GlassPanelBrush is PrimaryMid at an alpha over a
            PrimaryMid body: the same colour, which is why the strip read as one flat slab. A single flat
            Layer* tint had the same problem one shade lighter. The gradient carries the depth instead —
            the foreground colour at a bright alpha along the top edge, falling to almost nothing at the
            hairline the TabablzControl template draws — so the strip reads as a lit sheet on both light
            and dark palettes. Still one translucent layer per pixel: the tab, hover and caption-button
            overlays stack on this the way they already did.
        -->
        <Border Height="{x:Static host:TabWindowView.TITLE_BAR_HEIGHT}" VerticalAlignment="Top" Background="{DynamicResource TitleBarGlassBrush}" />

        <dragablz:TabablzControl x:Name="TabablzControl"
                                 BorderThickness="0"
                                 Margin="0" Padding="0"
                                 Background="Transparent"
                                 SelectionChanged="TabablzControl_OnSelectionChanged"
                                 SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                                 ItemsSource="{Binding Items}"
                                 HeaderMemberPath="DisplayName"
                                 PreviewMouseLeftButtonDown="TabablzControl_OnPreviewMouseLeftButtonDown"
                                 >
            <!-- important for docking -->
            <dragablz:TabablzControl.InterTabController>
                <dragablz:InterTabController InterTabClient="{Binding InterTabClient}" />
                <!--<dragablz:InterTabController />-->
            </dragablz:TabablzControl.InterTabController>
        </dragablz:TabablzControl>

        <!-- Function buttons (TitleBar Window Control Buttons) -->
        <StackPanel VerticalAlignment="Top">
            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Setter Property="Orientation" Value="Horizontal" />
                    <Setter Property="HorizontalAlignment" Value="Right" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SettingsPage.GeneralSettingViewModel.TabWindowCloseButtonOnLeft}" Value="True">
                            <Setter Property="HorizontalAlignment" Value="Left" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>
            <StackPanel.ContextMenu>
                <ContextMenu StaysOpen="False">
                    <CheckBox Content="{DynamicResource 'Place the close button on the left side to prevent accidental touches'}" IsChecked="{Binding SettingsPage.GeneralSettingViewModel.TabWindowCloseButtonOnLeft}"></CheckBox>
                </ContextMenu>
            </StackPanel.ContextMenu>

            <!-- Send command to sessions -->
            <Button Command="{Binding CmdSendCommand}"
                    ToolTip="{DynamicResource send_command_title}"
                    Visibility="{Binding SendCommandVisibility}"
                    Width="42" Height="{x:Static host:TabWindowView.TITLE_BAR_HEIGHT}"
                    VerticalAlignment="Top"
                    Style="{StaticResource WindowCaptionButtonStyle}">
                <Path Data="{StaticResource GeometryTerminal}"
                      Height="13" Width="13"
                      Fill="{Binding Foreground, RelativeSource={RelativeSource FindAncestor, AncestorType=Button}}"
                      Stretch="Uniform" />
            </Button>

            <!-- Pin Button (Always on top) -->
            <CheckBox ToolTip="{DynamicResource 'Always on top'}"
                      IsChecked="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}, Path=Topmost}">
                <CheckBox.Style>
                    <Style TargetType="CheckBox" BasedOn="{StaticResource CheckBoxBaseStyle}">
                        <Setter Property="Width" Value="42" />
                        <Setter Property="Height" Value="{x:Static host:TabWindowView.TITLE_BAR_HEIGHT}" />
                        <Setter Property="Focusable" Value="False" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate>
                                    <Border Name="Border" Background="{TemplateBinding Background}">
                                        <Path Height="12" Width="12" Stretch="Uniform"
                                              Fill="{DynamicResource PrimaryTextBrush}"
                                              Data="{StaticResource GeometryPin}"
                                              VerticalAlignment="Center" HorizontalAlignment="Center" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource LayerHoverBrush}" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="{DynamicResource LayerSelectedBrush}"></Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </CheckBox.Style>
            </CheckBox>

            <!-- Fullscreen Button -->
            <Button Command="{Binding CmdHostGoFullScreen}"
                    Width="42" Height="{x:Static host:TabWindowView.TITLE_BAR_HEIGHT}" VerticalAlignment="Top">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource WindowCaptionButtonStyle}">
                        <Setter Property="Visibility" Value="Visible"></Setter>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding WindowResizeMode}" Value="NoResize">
                                <Setter Property="Visibility" Value="Collapsed"></Setter>
                            </DataTrigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
                <Path Data="F1 M 82.500000 -82.500000 L 82.500000 -55.000000 L 77.000000 -55.000000 L 77.000000 -73.089844 L 14.910156 -11.000000 L 33.000000 -11.000000 L 33.000000 -5.500000 L 5.500000 -5.500000 L 5.500000 -33.000000 L 11.000000 -33.000000 L 11.000000 -14.910156 L 73.089844 -77.000000 L 55.000000 -77.000000 L 55.000000 -82.500000 Z "
                      Height="11" Width="11"
                      Fill="{Binding Foreground, RelativeSource={RelativeSource FindAncestor, AncestorType=Button}}" StrokeThickness="1" Stretch="Uniform" />
            </Button>

            <!-- Close All Button -->
            <Button ToolTip="{DynamicResource 'Close all'}" 
                    Command="{Binding CmdCloseAll}" 
                    Width="42" Height="{x:Static host:TabWindowView.TITLE_BAR_HEIGHT}" VerticalAlignment="Top"
                    Style="{StaticResource WindowCaptionCloseButtonStyle}"
                    Visibility="{Binding BtnCloseAllVisibility}">
                <Path Data="{StaticResource GeometryDelete}"
                      Height="12" Width="12"
                      Fill="{Binding Foreground, RelativeSource={RelativeSource FindAncestor, AncestorType=Button}}" Stretch="Uniform" />
            </Button>

            <!-- Minimize Button -->
            <Button Command="{Binding CmdGoMinimize}" 
                    Width="42" Height="{x:Static host:TabWindowView.TITLE_BAR_HEIGHT}" VerticalAlignment="Top"
                    Style="{StaticResource WindowCaptionButtonStyle}">
                <Rectangle Width="10" Height="1" Fill="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Button}, Path=Foreground}" />
            </Button>

            <!-- Maximize Button -->
            <Button Command="{Binding CmdGoMaximize}"
                    Width="42" Height="{x:Static host:TabWindowView.TITLE_BAR_HEIGHT}" VerticalAlignment="Top">
                <Button.InputBindings>
                    <MouseBinding Gesture="RightClick" Command="{Binding CmdGoMaximizeF11}" />
                </Button.InputBindings>
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource WindowCaptionButtonStyle}">
                        <Setter Property="Visibility" Value="Visible"></Setter>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding WindowResizeMode}" Value="NoResize">
                                <Setter Property="Visibility" Value="Collapsed"></Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
                <Grid>
                    <!-- Normal Window State Icon -->
                    <Path Height="10" Width="10"
                          Fill="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Button}, Path=Foreground}"
                          Data="F1 M 5.500000 -82.500000 L 82.500000 -82.500000 L 82.500000 -5.500000 L 5.500000 -5.500000 Z M 77.000000 -11.000000 L 77.000000 -77.000000 L 11.000000 -77.000000 L 11.000000 -11.000000 Z "
                          Stretch="Uniform">
                        <Path.Style>
                            <Style TargetType="Path">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}, Path=WindowState}" Value="Maximized">
                                        <Setter Property="Visibility" Value="Hidden"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Path.Style>
                    </Path>

                    <!-- Maximized Window State (Restore) Icon -->
                    <Path Height="10" Width="10"
                          Fill="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Button}, Path=Foreground}"
                          Data="M2,2 L2,0 L8,0 L8,6 L6,6 M0,2 L6,2 L6,8 L0,8 Z"
                          Stretch="Uniform">
                        <Path.Style>
                            <Style TargetType="Path">
                                <Setter Property="Visibility" Value="Hidden"></Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}, Path=WindowState}" Value="Maximized">
                                        <Setter Property="Visibility" Value="Visible"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Path.Style>
                    </Path>
                </Grid>
            </Button>

            <!-- Close Button -->
            <Button ToolTip="{DynamicResource Close}" 
                    Width="46" Height="{x:Static host:TabWindowView.TITLE_BAR_HEIGHT}" VerticalAlignment="Top"
                    Command="{Binding CmdClose}" 
                    Style="{StaticResource WindowCaptionCloseButtonStyle}">
                <Path Height="10" Width="10"
                      Fill="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Button}, Path=Foreground}"
                      Data="{StaticResource GeometryCloseCross}"
                      Stretch="Uniform">
                </Path>
            </Button>
        </StackPanel>

        <ContentControl s:View.Model="{Binding TopLevelViewModel}"></ContentControl>
    </Grid>
</styles:WindowChromeBase>
