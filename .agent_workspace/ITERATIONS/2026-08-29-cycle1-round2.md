# Cycle 1 · Round 2 — windowless fixes and test cover

- **Time:** 2026-08-29 (UTC)
- **Cycle / Round:** 1 / 2 (deep optimization + implementation)
- **Model:** all agents `inherit` -> Claude Opus 4.8; cloud fan-out unavailable (quota), so run one editing agent at a time
- **Branch:** `cursor/agent-env-and-orchestrator-167c`

## Search / research

Confirmed the natural-sort chunking approach (Wikipedia/Alphanum: compare digit runs by magnitude, text
case-insensitively) and IP sorting by `GetAddressBytes()` byte comparison with `IPAddress.TryParse` handling
compressed IPv6 (RFC 5952 background). Used to fix the server-list comparer.

## Changes (each verified by the §7.2 harness + full build)

| # | Change | Tests |
| --- | --- | --- |
| 1 | `HostNaturalSort` (new) + `SubTitleSortByNaturalIp` thin wrapper — IPv6/port/direction sort bugs fixed | 9 |
| 2 | `ProcessArgumentEscaper` regression tests (security guard, no code change) | 7 |
| 3 | `MRemoteNgCsv` (extracted) — bounds-check fixes the short-row import crash | 6 |
| 4 | `ProxyHandshake` tests (SOCKS5/4/4a/HTTP-CONNECT), widened to public | 12 |

## Verification (honest)

- `dotnet build Tests/Tests.csproj -c Debug -p:EnableWindowsTargeting=true` → 0 errors.
- §7.2 throwaway `net9.0` harness over the four new fixtures + files under test → **34 passed, 0 failed**.
- Full 507-method suite not run here (needs `Microsoft.WindowsDesktop.App`); runs in CI on `windows-latest`.
- `Ui/AppVersion.cs` untouched; no hardening reverted; no new user-visible strings.

## Risks / not done

- Two changes are behaviour-affecting (server-list order; import no longer aborts) — both are corrections;
  Windows CI is the gate before merge.
- Deferred: IPv6 `ProxyJump` parsing in `SshConfigParser`; quote-aware mRemoteNG CSV splitting.

## Next round

Take the deferred SSH `ProxyJump` IPv6 fix, or a fresh reconnaissance pass once Windows CI on this branch is
green and the branch is merged/released.
